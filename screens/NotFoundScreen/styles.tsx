import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        marginTop: 5,
        padding: 35
    },
    buttonContainer: {
        marginTop: 15,
    },
    errorText: {
        fontSize: 25,
        fontWeight: "bold"
    },
    imgNF: {
        borderRadius: 30,
        width: '100%',
        maxHeight: 400,
        height: '100%'
    },
    imgContainer: {
        marginTop: 10,
        padding: 15
    }
});

export default styles;